function longestPalindrome(str) {
  var words = str.split(" ");
  for (var i=0; i < word.length; i++) {
    for (var j=0; j < word[i].length/2; j++) {
      if (word[i][j] !== word[i][word[i].length - 1 - j]) {
        
      }
    }
  }
}